# pnpm configuration
# Tự động tạo symlink cho hoisted dependencies
shamefully-hoist=false
# Strict peer dependencies để tránh lỗi
strict-peer-dependencies=true
# Sử dụng hard links để tiết kiệm dung lượng
prefer-symlinked-executables=false
# Auto install peers
auto-install-peers=true

